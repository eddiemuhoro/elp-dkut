import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  gridItem: {
    padding: '10px'
  },
  buttons: {
    display: 'flex',
    justifyContent:'flex-start',
    
  }
  
});

